<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Display User</title>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td,
        th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }

        .button {
            background-color: #4CAF50;
            border: none;
            color: white;
            padding: 15px 24px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin-left: 16px;
            border-radius: 12px;
            cursor: pointer;
        }

        .btn-delete {
            background-color: #f44336;
        }
    </style>
</head>

<body>
    <div style="margin-top: 30px;">
        <h1>User Infomation</h1>
        <table>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Email</th>
                <th>Action</th>
            </tr>

            <% for(let i=0; i < userData.length; i++) {%>
                <tr>
                    <td>
                        <%= userData[i].id%>
                    </td>
                    <td>
                        <%= userData[i].username%>
                    </td>
                    <td>
                        <%= userData[i].email%>
                    </td>
                    <td>
                        <a href="/edit-crud?id=<%= userData[i].id %>" class="button btn-edit" type="button">Edit</a>
                        <a href="/delete-crud?id=<%= userData[i].id %>" class="button btn-delete"
                            type="button">Remove</a>
                    </td>
                </tr>
                <%} %>

        </table>
    </div>
</body>

</html>